stoi convert string to integer in 682

if vector string then use " "
else use ' ' or use " " for strings

Important denoted by -------------------------------

EXPLORE ERASE FUNCTION 

explore every type of type of function in cpp------------------------------

if dont inittalize size then can do push back for vector....

hashing concept

que 11 must be understant-------

learn vectro of vectors concept

sort(nums.begin(),nums.end()); for sorting-------------

use     -----------#include<bits/stdc++.h>--------- and using namespace std;

insert and resize functuon is very important....

*max_element(sum.begin(), sum.end())



vector<double> sum(n - k + 1, 0.0);
means:
👉 Create a vector named sum
👉 with (n - k + 1) elements
👉 and initialize EVERY element with 0.0

 swap(nums[j],nums[j+1]);

 erase function important
 acuumulate STL important

 Q 39 main inittialisation ko samjho

vector<vector<int>> matrix(m, vector<int>(n, 0)); initialise all emlemnet 0
 
----------linked list code in C folder is  very  important ------------


que 202 unordrerd set is very imp

instead of:
for(char c : sentence) {
    letters.insert(c);
}
You can absolutely write:
for(int i = 0; i < sentence.length(); i++) {
    letters.insert(sentence[i]);
}


stringstream ss(text);
Extract words one by one
string word;
vector<string> words;

while(ss >> word) {
    words.push_back(word);
}

vector.insert(position, first_iterator, last_iterator);

    v.erase(v.begin() + i);

    2 pointer concept is very important like in question number 392 as leet code


    int x = 7;
string s = to_string(x);


que 228 leetcode condiyion of empty array very imp

in 62_448 pls learn the optimal trick for interviews !!!

sort(v.begin(), v.end());
v.erase(unique(v.begin(), v.end()), v.end()); in que 63 vs code
string.erase(position, length);